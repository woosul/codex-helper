interface:
  display_name: "Feature Delivery"
  short_description: "Explicit, root-first feature delivery workflow"
  default_prompt: "Use $feature-delivery for this task. Subagents are authorized; if you delegate, let each subagent perform its assigned scope."

policy:
  allow_implicit_invocation: false
